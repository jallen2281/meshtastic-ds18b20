; rpi3bplus_rfm95_oled env is defined in variants/portduino/platformio.ini
; so that portduino_base (from arch/portduino/portduino.ini) resolves correctly
; via the root platformio.ini extra_configs = arch/*/*.ini glob.
